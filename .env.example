# ImageKit — clé **privée** (Dashboard → Developer options → API keys → Private key).
# Obligatoire pour signer les uploads depuis l’app. Ne commitez pas le fichier .env réel.
EXPO_PUBLIC_IMAGEKIT_PRIVATE_KEY=
